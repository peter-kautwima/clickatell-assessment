fastapi
uvicorn[standard]
python-dotenv
pydantic-settings  # BaseSettings for config.py's env-driven LLM toggle
numpy              # chunk/vector math; also sentence-transformers' own dependency
sentence-transformers  # local embeddings (D2) — all-MiniLM-L6-v2, no paid API
pytest              # test runner (rule 4)
pytest-cov          # coverage reporting toward the 90% bar (rule 4)
