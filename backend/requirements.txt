# Headers show which branch added each dep (CLAUDE.md rule 10); D-numbers
# point to DECISIONS.md; fuller per-package notes (private, mine) are in
# notes/CRASH_COURSE.md §5b.

# --- core / pre-existing (before any feature branch) ---
fastapi
uvicorn[standard]
python-dotenv       # loads .env into environment variables at startup

# --- feat/chunking-embeddings ---
pydantic-settings   # BaseSettings backing config.py's env-driven LLM toggle
numpy               # vector math for similarity search — DECISIONS.md D3 (Vector storage & search)
sentence-transformers  # local embedding model — DECISIONS.md D2 (Embedding model)
pytest              # test runner (CLAUDE.md rule 4)
pytest-cov          # coverage measurement toward the 90% bar (CLAUDE.md rule 4)
httpx2              # silences starlette.testclient's httpx deprecation warning; test-only

# --- feat/storage-documents (built — added no dependencies) ---

# --- feat/ask-llm (planned) ---
# anthropic         # official SDK for the live LLM call
