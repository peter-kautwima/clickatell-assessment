# Grouped by the branch that introduced each dependency (CLAUDE.md rule 10).
# Full reasoning for each package lives in notes/CRASH_COURSE.md §5b — this
# file only labels *when* it arrived, not *why*, so there's one place that
# can drift instead of two.

# --- core / pre-existing (before any feature branch) ---
fastapi
uvicorn[standard]
python-dotenv       # loads .env into environment variables at startup

# --- feat/chunking-embeddings ---
pydantic-settings   # BaseSettings backing config.py's env-driven LLM toggle
numpy               # vector math — also central to feat/storage-documents next
sentence-transformers  # local embedding model — see D2
pytest              # test runner (rule 4)
pytest-cov          # coverage measurement toward the 90% bar (rule 4)
httpx2              # silences starlette.testclient's httpx deprecation warning; test-only

# --- feat/storage-documents (planned — nothing new expected yet) ---

# --- feat/ask-llm (planned) ---
# anthropic         # official SDK for the live LLM call
