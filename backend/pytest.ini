[pytest]
pythonpath = .
markers =
    evaluation: quality harness on the real embedding model (run: pytest -m evaluation)
; Deselected by default so the ordinary suite stays fast on mocked
; embeddings; a trailing -m on the command line overrides this.
addopts = -m "not evaluation"
